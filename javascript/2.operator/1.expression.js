// 리터럴 (literal) : 코드에서 값을 나타내는 표기법

// 문 (statement) : 최소 실행 단위

// 표현식 (expression) : 값으로 평가될 수 있는 문

1; // 숫자 리터럴 표현식
1 + 1; // 연산자 표현식
function call() {}
call; // 함수 호출 표현식
let b; // 선언문
b = 2; // 할당문, 할당 표현식인 문

let a = (b = 2);
console.log(a);
